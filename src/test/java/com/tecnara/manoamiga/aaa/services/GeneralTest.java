/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.tecnara.manoamiga.aaa.services;

import java.util.Date;
import org.junit.jupiter.api.Test;
//import com.tecnara.manoamiga.aaa.services.General;

import static org.junit.jupiter.api.Assertions.*;

/**
 *
 * @author PC
 */
public class GeneralTest {
    
    public GeneralTest() {
    }
/*
    @Test
    public void testGetDaysAgo() {
        General g=new General();
        Date fecha = g.getFechaActual();
        General instance = new General();

        String result = instance.getDaysAgo(fecha);
        assertEquals("Hoy", result);

        result = instance.getDaysAgo(g.getFechaCercana(1));
        assertEquals("Dentro de 1 día", result);

        result = instance.getDaysAgo(g.getFechaCercana(2));
        assertEquals("Dentro de 2 días", result);

        result = instance.getDaysAgo(g.getFechaCercana(31));
        assertEquals("Dentro de 1 mes", result);

        result = instance.getDaysAgo(g.getFechaCercana(61));
        assertEquals("Dentro de 2 meses", result);

        result = instance.getDaysAgo(g.getFechaCercana(-1));
        assertEquals("Hace 1 día", result);

        result = instance.getDaysAgo(g.getFechaCercana(-2));
        assertEquals("Hace 2 días", result);

        result = instance.getDaysAgo(g.getFechaCercana(-31));
        assertEquals("Hace 1 mes", result);

        result = instance.getDaysAgo(g.getFechaCercana(-61));
        assertEquals("Hace 2 meses", result);
    }
*/
}
